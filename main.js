const { app, BrowserWindow } = require('electron'); function createWindow() { const win = new BrowserWindow({ width: 1280, height: 800, title: "FlixCasa Admin Panel", autoHideMenuBar: true }); win.loadURL('https://flix-casa-pro-v2.vercel.app/admin'); } app.whenReady().then(createWindow); app.on('window-all-closed', () = if (process.platform !== 'darwin') app.quit(); }); 
